import React from 'react'
import PropTypes from 'prop-types'

class Consumption extends React.Component {
  render () {
    return(
      <h2>This will show the Consumption</h2>
    );
  }
}

export default Consumption;
