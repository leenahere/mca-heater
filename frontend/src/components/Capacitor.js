import React from 'react'
import PropTypes from 'prop-types'

class Capacitor extends React.Component {
  render () {
    return(
      <h2>This will show the Capacitor Loading Curve</h2>
    );
  }
}

export default Capacitor;
